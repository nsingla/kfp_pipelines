* In a one level Pipeline (no control flow)
*  OneOF
    * Return OneOF in a Dag
    * Use OneOf returned in a Dag as input in another task
    * Pass OneOF as Task Input within same level
*  Conditions
    * dsl.If on hardcoded
    * dsl.If on TaskInput
    * dsl.If on ComponentInput
*  ParallelFor
    * Iterate over hardcoded values
    * Iterate over ComponentInput Parameters
    * Iterate over Task Input Parameters
    * Iterate over ComponentInput Artifacts
    * Iterate over Task Input Artifacts
* ExitHandler
    * Pass TaskFinalStatus
* Importer case